<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.josungryong.graduateproject3">
    <uses-permission android:name="android.permission.INTERNET"></uses-permission>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="OSD"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

            <activity
                android:name=".Login"
                android:label=""
                android:theme="@style/AppTheme.NoActionBar">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".MainActivity"
            android:theme="@style/AppTheme.NoActionBar">
        </activity>

        <activity
            android:name=".MyPage.MyPage"
            android:theme="@style/AppTheme.NoActionBar">
        </activity>
        <activity android:name=".join1"></activity>

        <activity
            android:name=".join2"></activity>
        <activity
            android:name=".join3"></activity>
        <activity
            android:name=".join4"></activity>

        <activity android:name=".Project_Fragment.ProjectInfo"></activity> // 프로젝트 상세페이지
        <activity android:name=".Design_Fragment.DesignInfo"
            android:theme="@style/AppTheme.NoActionBar">
        </activity> // 디자인 상세페이지
        <activity android:name=".TestServer"></activity>
        <activity android:name=".Design_Fragment.DesignWrite"></activity><!-- ATTENTION: This was auto-generated to add Google Play services to your project for
     App Indexing.  See https://g.co/AppIndexing/AndroidStudio for more information. -->
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />
        // 디자인 등록
    </application>

</manifest>